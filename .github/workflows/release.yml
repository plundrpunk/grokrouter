name: Prepare signed public release

on:
  workflow_dispatch:
    inputs:
      include_windows:
        description: Include signed Windows preview installers in the draft
        required: true
        type: boolean
        default: false

permissions:
  contents: read

env:
  PUBLIC_RELEASE_REPOSITORY: promptadvisers/grokrouter-downloads

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
      - name: Install pinned runtime dependencies
        run: npm ci --prefix runtime --ignore-scripts --no-audit --no-fund
      - name: Import Developer ID certificate
        env:
          CERTIFICATE_P12_BASE64: ${{ secrets.APPLE_DEVELOPER_ID_P12_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.APPLE_DEVELOPER_ID_P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.APPLE_BUILD_KEYCHAIN_PASSWORD }}
        run: |
          CERTIFICATE_PATH="$RUNNER_TEMP/developer-id.p12"
          KEYCHAIN_PATH="$RUNNER_TEMP/grokbot-router.keychain-db"
          echo "$CERTIFICATE_P12_BASE64" | base64 --decode > "$CERTIFICATE_PATH"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERTIFICATE_PATH" -P "$CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          security list-keychain -d user -s "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
      - name: Configure Apple notarization
        env:
          APPLE_ID: ${{ secrets.APPLE_NOTARY_APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_NOTARY_APP_PASSWORD }}
        run: >-
          xcrun notarytool store-credentials grokbot-router-notary
          --apple-id "$APPLE_ID"
          --team-id "$APPLE_TEAM_ID"
          --password "$APPLE_APP_PASSWORD"
      - name: Test and build signed, notarized app
        env:
          ROUTER_CODESIGN_IDENTITY: Developer ID Application
          ROUTER_NOTARY_PROFILE: grokbot-router-notary
        run: npm run build
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-macos
          path: |
            build/grokrouter-*-macos.dmg
            build/grokrouter-*-macos.dmg.sha256
            build/grokrouter-*-macos.zip
            build/grokrouter-*-macos.zip.sha256
          if-no-files-found: error

  windows:
    if: ${{ inputs.include_windows }}
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 26
          cache: npm
          cache-dependency-path: |
            runtime/package-lock.json
            installer-windows/package-lock.json
      - uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
        with:
          python-version: "3.13"
      - name: Require Windows signing certificate
        shell: pwsh
        env:
          WINDOWS_CODESIGN_PFX_BASE64: ${{ secrets.WINDOWS_CODESIGN_PFX_BASE64 }}
        run: |
          if (-not $env:WINDOWS_CODESIGN_PFX_BASE64) { throw "Configure WINDOWS_CODESIGN_PFX_BASE64 before publishing Windows builds." }
          [IO.File]::WriteAllBytes("$env:RUNNER_TEMP\grokrouter-codesign.pfx", [Convert]::FromBase64String($env:WINDOWS_CODESIGN_PFX_BASE64))
      - name: Install pinned runtime dependencies
        shell: bash
        run: npm ci --prefix runtime --ignore-scripts --no-audit --no-fund
      - name: Test Windows-portable layers
        shell: bash
        run: npm run test:runtime && npm run test:windows
      - name: Build and sign x64 and Arm64 Windows packages
        shell: bash
        env:
          ROUTER_WINDOWS_SIGN_PFX: ${{ runner.temp }}\grokrouter-codesign.pfx
          ROUTER_WINDOWS_SIGN_PASSWORD: ${{ secrets.WINDOWS_CODESIGN_PFX_PASSWORD }}
          ROUTER_WINDOWS_REQUIRE_SETUP: "1"
        run: |
          npm run build:windows -- x64
          npm run build:windows -- arm64
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-windows
          path: |
            build/grokrouter-*-windows-x64.zip
            build/grokrouter-*-windows-x64.zip.sha256
            build/grokrouter-*-windows-arm64.zip
            build/grokrouter-*-windows-arm64.zip.sha256
            build/grokrouter-*-windows-x64-setup.exe
            build/grokrouter-*-windows-x64-setup.exe.sha256
            build/grokrouter-*-windows-arm64-setup.exe
            build/grokrouter-*-windows-arm64-setup.exe.sha256
          if-no-files-found: error

  release:
    needs: [macos, windows]
    if: ${{ always() && needs.macos.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 26
      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          pattern: release-*
          path: release-assets
          merge-multiple: true
      - name: Require public release access
        env:
          PUBLIC_RELEASE_TOKEN: ${{ secrets.PUBLIC_RELEASE_TOKEN }}
        run: test -n "$PUBLIC_RELEASE_TOKEN"
      - name: Create public download draft
        env:
          GH_TOKEN: ${{ secrets.PUBLIC_RELEASE_TOKEN }}
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          ASSETS=(
            release-assets/*.dmg
            release-assets/*.dmg.sha256
            release-assets/*-macos.zip
            release-assets/*-macos.zip.sha256
          )
          if compgen -G 'release-assets/*-setup.exe' > /dev/null; then
            ASSETS+=(
              release-assets/*-setup.exe
              release-assets/*-setup.exe.sha256
              release-assets/*-windows-*.zip
              release-assets/*-windows-*.zip.sha256
            )
          fi
          gh release create "v$VERSION" \
            --repo "$PUBLIC_RELEASE_REPOSITORY" \
            "${ASSETS[@]}" \
            --title "GrokRouter v$VERSION" \
            --notes-file RELEASE_NOTES.md \
            --draft
